<!DOCTYPE html>
html(lang="es")
    include common/head.pug
    // Theme included stylesheets
    link(href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet")
    link(href="https://cdn.quilljs.com/1.3.6/quill.bubble.css" rel="stylesheet")
    style.
        #editor{
            width:100%;
            height: 100vh;
            display:none;
        }
    body
        include common/nav.pug
        .container-fluid
            .row.d-none(data-loader-success)
                .col-12.jh-slot.no-border
                    .alert.alert-success(role="alert")
                        span Guardado
                        span &nbsp;
                        i.far.fa-thumbs-up
            .row.d-none(data-loader)
                .col-12.jh-slot.no-border
                    .alert.alert-info(role="alert")
                        span Guardando
                        span &nbsp;
                        i.fas.fa-spinner.fa-pulse
            .row
                .col-12.jh-slot.no-border
                    .btn-group.float-right
                        button.btn.btn-danger(data-load-view data-view="main" data-size="self") Cancelar
                        button.btn.btn-success(data-btn-save) Guardar
            .row
                .col-md-4.order-md-2.col-xs-12
                    .jh-slot
                        h3 Título
                        input(class="form-control" type="text" value="" placeholder="title" data-post="title")
                    .jh-slot
                        h3 Resumen
                        textarea(class="form-control" type="text" value="" placeholder="summary" data-post="summary")
                    .jh-slot
                        h3 Url bonita
                        input(class="form-control" type="text" value="" placeholder="pretty url" data-post="pretty")
                    .jh-slot
                        h3 Tags
                        .badge-list(data-tag-list)
                        button.btn.btn-outline-primary.btn-lg.btn-block(type="button" data-load-view data-view="addTagToPost" data-size="largeVertical") Agregar
                    //- .image
                    //-     h3 main image
                    //-     .input-group.mb-3
                    //-         .custom-file
                    //-         input(type="file" class="custom-file-input" id="inputGroupFile02")
                    //-         label(class="custom-file-label" for="inputGroupFile02") Choose file
                .col-md-8.order-md-1.col-xs-12
                    .jh-slot
                        h3 Contenido
                        textarea#editor.form-control(data-post="body")
                        #editorQuill
        include common/footer.pug
        // Main Quill library
        script(src="https://cdn.quilljs.com/1.3.6/quill.min.js")
        script
            include js/editor.js